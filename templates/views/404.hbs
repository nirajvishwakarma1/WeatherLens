{{>header}}
<section class="py-5">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1 class="text-center display-1">4<i class="fa fa-ban"></i>4</h1>
                <p class="text-center">{{message}}</p>
            </div>
        </div>
    </div>
</section>
{{>footer}}